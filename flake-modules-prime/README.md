This directory contains the flake parts flake modules that will provide outputs
for the base flake. See ARCHITECTURE.md at the top of the repo.

# Current name

For migration purposes I will keep the original files as they are so I have an
easier time running diffs. This directory has a `prime` postfix to indicate that
it's related to `flake-modules`
